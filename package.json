{
  "name": "contractshield",
  "private": true,
  "workspaces": [
    "packages/*",
    "pro/*",
    "tools/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "golden-test": "node tools/golden-pdp-runner.mjs"
  },
  "devDependencies": {
    "yaml": "^2.8.2"
  }
}
