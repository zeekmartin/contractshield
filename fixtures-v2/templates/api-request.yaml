# Base template for API request contexts
# All fixtures inherit from this unless they specify a different _template
version: "0.1"
timestamp: "2026-01-17T00:00:00.000Z"

request:
  method: POST
  path: /api/license/activate
  routeId: license.activate.v1
  query: {}
  headers:
    content-type: application/json
  contentType: application/json
  body:
    present: true
    sizeBytes: 180
    sha256: fixture-sha256
    json:
      redacted: true
      sample:
        tenantId: t-1
        licenseKey: "***"
        deviceId: d-1

identity:
  authenticated: true
  subject: u-1
  tenant: t-1
  scopes:
    - license:activate

client:
  ip: 203.0.113.10
  userAgent: fixture

runtime:
  language: node
  service: test
  env: test
